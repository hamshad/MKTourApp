---
phase: 11.1-booking-screen-bottom-sheet-ui-compaction
plan: "01"
subsystem: ui
tags: [flutter, booking, bottom-sheet, payment-toggle, ride-hail-ui]

# Dependency graph
requires:
  - phase: 11-upfront-payments
    provides: [mandatory paymentMethod booking contract, instant paymentUrl WebView routing, link-vs-cash branch]
provides:
  - Compact bottom-sheet card on RideConfirmationScreen (fare row + single payment row + Confirm CTA with price)
  - Visual-parity compaction on legacy ConfirmBookingScreen
  - Booking contract byte-identical (mandatory paymentMethod, link→WebView / cash→matching)
affects: [booking-qa, ride-assigned-display]

# Tech tracking
tech-stack:
  added: []
  patterns: [ride-hail compact bottom-sheet card, single expandable payment-method row, Confirm-CTA-with-price plus compact Schedule affordance]

key-files:
  created: []
  modified:
    - lib/features/booking/ride_confirmation_screen.dart
    - lib/features/booking/confirm_booking_screen.dart

key-decisions:
  - "Expandable payment row (tap to reveal Online/Cash) replaces the always-visible two-button toggle; writes existing state, no new source of truth"
  - "Fare-variance note folded to single-line caption with full-text Tooltip on main screen; payment info box left untouched on legacy screen"
  - "Confirm CTA carries price (Confirm • £X); Schedule demoted to compact outlined button (main) / small text-button (legacy), hidden for prebooked rides per prior rule"

patterns-established:
  - "Single payment row idiom: icon + current-choice label + sub-label + chevron, expands inline to Online/Cash options"
  - "Bottom-sheet card: drag handle, rounded top corners, SafeArea top:false, 50px primary CTA"

requirements-completed: [UI-11.1-01, UI-11.1-02, UI-11.1-03]

# Metrics
duration: 2min
completed: 2026-09-17
---

# Phase 11.1 Plan 01: Booking Screen Bottom-Sheet UI Compaction Summary

**Compact ride-hail bottom-sheet cards on both booking screens — fare row, single expandable Online/Cash row, price-bearing Confirm CTA — with Phase 11 booking contract untouched**

## Performance

- **Duration:** 2 min
- **Started:** 2026-09-17T10:48:12Z
- **Completed:** 2026-09-17T10:49:37Z
- **Tasks:** 3
- **Files modified:** 2

## Accomplishments

- RideConfirmationScreen bottom section is one compact card: drag handle, category/ETA + price fare row, single tappable payment row expanding to Online/Cash, Confirm • £X CTA with compact Schedule button beside it
- Legacy ConfirmBookingScreen mirrors the idiom: expandable payment row, full-width Confirm Booking • £X CTA, Schedule for Later as a small text-button; paddings tightened (24 → 16)
- Booking contract verified intact: mandatory paymentMethod on every bookRideWithPayment call, link→PaymentWebViewScreen / cash→direct flow preserved, diff limited to the two UI files, analyze clean

## Task Commits

Each code task was committed atomically (Task 3 was read-only verification, no code change):

1. **Task 1: Compact bottom-sheet card on RideConfirmationScreen** - `712705d` (feat)
2. **Task 2: Parity compaction on legacy ConfirmBookingScreen** - `65ed6e5` (feat)
3. **Task 3: Verify booking contract intact, no behavior/API change** - no commit (verification only, no edits needed)

**Plan metadata:** `a855fce` (docs: complete plan)

## Files Created/Modified

- `lib/features/booking/ride_confirmation_screen.dart` - Compact bottom-sheet card: `_buildBottomBar` rewrite (drag handle, fare row, single-line variance caption, Confirm • price + compact Schedule), `_buildPaymentMethodSelector` as expandable single row, `_buildPaymentMethodOption` tightened, new `_buildCompactPrice` preserving promo FREE/discount rendering
- `lib/features/booking/confirm_booking_screen.dart` - Parity compaction: expandable payment row idiom, rounded action card with full-width Confirm Booking • £X CTA + Schedule text-button, scroll padding 24 → 16

## Decisions Made

- Expandable payment row writes the existing `_selectedPaymentMethod` state (plus `_paymentTiming` sync on legacy, as before); no new state source, no shared widget extract — each screen keeps its file-local selector per Phase 11 convention.
- Main screen keeps promo FREE/discount rendering inside the compact price block (original-fare strikethrough + gift icon; per-line discount amount dropped for space, full variance note available via Tooltip).
- Schedule affordance differs by screen per their existing patterns: compact outlined button beside Confirm (main) vs small text-button under Confirm (legacy) — both hidden/gated exactly as before (prebooked rides hide it on main; `_isLoading` disables everywhere).

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None - `_confirmRide`, `_confirmPrebook`, `_processBooking`, `_showScheduleSheet`, `_handleScheduledPayment`, `_switchScheduledPayment`, `_confirmBooking`, `_handleScheduleRide`, success/error dialogs, and notes/stops sections all untouched. Pre-existing working-tree deletions (`flutter_new_flow.md`, `prebook_new_flow.md`) predate this plan and were not staged or committed.

## Verification

- `flutter analyze` on both files: no errors (21 infos/warnings total, all pre-existing: withOpacity deprecations, unused socket import, intl depend_on_referenced_packages)
- `rg "paymentMethod"` both screens: mandatory paymentMethod still passed into every `bookRideWithPayment` call (`paymentMethod: paymentMethod` / `paymentMethod: _selectedPaymentMethod` / `paymentMethod: payload.paymentMethod`)
- `rg "PaymentWebViewScreen"` lib/features/booking/: instant + scheduled link branches still push WebView immediately on both screens; cash paths contain no WebView push
- `git diff --stat HEAD~2..HEAD`: only the two intended booking screen files changed

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Bottom-sheet compaction complete on both booking paths; ready for manual QA: bottom card roughly half prior height, map visible above, payment row expands to Online/Cash, Confirm carries price, Schedule affordance works, no overflow at 320px width
- Booking behavior identical to Phase 11; no API/payload/routing/payment-logic regression

---
*Phase: 11.1-booking-screen-bottom-sheet-ui-compaction*
*Completed: 2026-09-17*

## Self-Check: PASSED
