---
phase: 11.1-booking-screen-bottom-sheet-ui-compaction
verified: 2026-09-17T11:00:00Z
status: human_needed
score: 3/3 must-haves verified
human_verification:
  - test: "Open RideConfirmationScreen, check bottom card roughly half prior height with map visible above"
    expected: "Drag handle + one fare row + single payment row + Confirm CTA with price visible; map not pushed off-screen"
    why_human: "Vertical compactness and map visibility are visual; code structure confirms layout but not rendered pixels"
  - test: "Tap payment row, pick Online then Cash, confirm each"
    expected: "Row expands to Online/Cash; Online opens PaymentWebView immediately; Cash goes straight to driver-matching"
    why_human: "Runtime routing behavior needs on-device QA"
  - test: "Check bottom card at 320px width, confirm no overflow"
    expected: "No overflow errors; text ellipsizes/scales via Flexible/FittedBox"
    why_human: "Overflow only manifests at render time on small screens"
---

# Phase 11.1: Booking Screen Bottom-Sheet UI Compaction Verification Report

**Phase Goal:** Ride booking screen bottom section compacted into an Uber/Lyft/Bolt-inspired bottom sheet with map visible above, Phase 11 booking contract intact.
**Verified:** 2026-09-17T11:00:00Z
**Status:** human_needed (all automated checks pass; visual/runtime QA needs human)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Booking screen bottom section is a single compact ride-hail-style card — fare row, one tappable Online/Cash payment row, Confirm CTA with price + compact Schedule affordance — with the map visible above | ✓ VERIFIED | `ride_confirmation_screen.dart` `_buildBottomBar` (line 1662): drag handle, fare row (category/ETA left, `_buildCompactPrice` right, lines 1737-1801), single-line variance caption + Tooltip (1803-1833), single `_buildPaymentMethodSelector` row (1836-1839), Confirm `Confirm • £X` + compact outlined Schedule button (1841-1976), `SafeArea(top:false)`, rounded top corners. `build()` pins it below `Expanded` scroll containing `_buildMapSnapshot` (lines 929-968) so map stays above. Legacy `confirm_booking_screen.dart`: expandable payment row (435-524), rounded action card with `Confirm Booking • £X` + Schedule text-button (955-1063), map `SizedBox(200)` above scroll content. |
| 2 | Online (payment_link) still opens the payment WebView immediately at booking; Cash still goes straight to driver-matching | ✓ VERIFIED | Main `_processBooking` link branch pushes `PaymentWebViewScreen` (lines 594-602) gated on success, cash path pushes `RideAssignedScreen` directly (644-660). Scheduled link branch via `_handleScheduledPayment` pushes `PaymentWebViewScreen` (764-770). Legacy `_confirmBooking` identical idiom (lines 107-115 WebView on link, 135 success dialog on cash), `_handleScheduledPayment` (274-280). `rg PaymentWebViewScreen` hits all four call sites, none on cash paths. |
| 3 | No API, payload, routing, or payment-logic change: booking POST still carries mandatory paymentMethod exactly as Phase 11 built it | ✓ VERIFIED | `rg paymentMethod`: main passes `paymentMethod: paymentMethod` into `bookRideWithPayment` (line 535), `_confirmRide`/`_confirmPrebook`/schedule callers pass `_selectedPaymentMethod` / `payload.paymentMethod` (296, 308, 341, 435, 444, 844, 859). Legacy passes `paymentMethod: _selectedPaymentMethod` (74) and `paymentMethod: payload.paymentMethod` (213). Untouched handlers confirmed present: `_confirmRide`, `_confirmPrebook`, `_processBooking`, `_showScheduleSheet`, `_handleScheduledPayment`, `_switchScheduledPayment`, `_confirmBooking`, `_handleScheduleRide`. Commits `712705d` + `65ed6e5` touch only the two screen files. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `lib/features/booking/ride_confirmation_screen.dart` | Compact bottom-sheet card, contains `_buildBottomBar` | ✓ VERIFIED | Exists, 2033 lines, substantive (not stub). Contains `_buildBottomBar`, `_buildPaymentMethodSelector`, `_buildCompactPrice`. Wired: `_buildBottomBar()` called in `build()` line 966. |
| `lib/features/booking/confirm_booking_screen.dart` | Visual-parity compaction, contains `_buildPaymentMethodSelector` | ✓ VERIFIED | Exists, 1068 lines, substantive. Contains `_buildPaymentMethodSelector` (435), `_buildPaymentMethodOption`, compact action card. Wired: selector called line 949, Confirm/Schedule call `_confirmBooking`/`_showScheduleSheet`. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `ride_confirmation_screen.dart` | `PaymentWebViewScreen` | `_processBooking` link branch pushes `PaymentWebViewScreen` on success-gated flow | WIRED | `PaymentWebViewScreen(` at 597 (instant) + 768 (scheduled); pattern `PaymentWebViewScreen` matched. Cash path has no WebView push. |
| `ride_confirmation_screen.dart` | `bookRideWithPayment` | `_processBooking` passes `_selectedPaymentMethod` as required paymentMethod | WIRED | `paymentMethod: paymentMethod` at 535; callers supply `_selectedPaymentMethod` / `payload.paymentMethod`. Pattern `paymentMethod: _selectedPaymentMethod\|paymentMethod: paymentMethod` matched (lines 74, 213 legacy; 341, 535 main). |

### Requirements Coverage

No `REQUIREMENTS.md` exists in repo (confirmed — IDs verified against PLAN frontmatter + SUMMARY instead, per task note).

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| UI-11.1-01 | 11.1-01-PLAN.md | Compact bottom-sheet card on RideConfirmationScreen | ✓ SATISFIED | `_buildBottomBar` rewrite verified above; commit `712705d` (1 file, +429/-275). |
| UI-11.1-02 | 11.1-01-PLAN.md | Parity compaction on legacy ConfirmBookingScreen | ✓ SATISFIED | Expandable payment row + compact action card verified; commit `65ed6e5` (1 file, +168/-88). |
| UI-11.1-03 | 11.1-01-PLAN.md | Booking contract intact, no behavior/API change | ✓ SATISFIED | paymentMethod + WebView wiring verified; `flutter analyze` clean (no errors, 21 pre-existing infos); diff limited to two UI files. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | `TODO/FIXME/PLACEHOLDER/Not implemented/return null` | — | None found in either file (rg clean). |
| — | — | `flutter analyze` errors | — | None. 21 infos/warnings total, all pre-existing (`withOpacity` deprecations, unused `_paymentTiming` field, socket import) — documented in SUMMARY, no blockers. |
| `ride_confirmation_screen.dart` | 63 | `unused_field _paymentTiming` warning | ℹ️ Info | Pre-existing; still assigned in `_processBooking`, harmless. |

### Human Verification Required

### 1. Compact card visual check

**Test:** Open RideConfirmationScreen; observe bottom card height vs map.
**Expected:** Fare row + single payment row + Confirm CTA in roughly half prior vertical space; map visible above.
**Why human:** Rendered pixels can't be verified by grep.

### 2. Payment routing runtime check

**Test:** Tap payment row → expand → select Online, confirm; repeat with Cash.
**Expected:** Online opens payment WebView immediately; Cash goes straight to driver-matching/success.
**Why human:** Requires on-device runtime flow.

### 3. Small-screen overflow check

**Test:** Render bottom card at 320px width.
**Expected:** No overflow; Flexible/FittedBox/ellipsis handle tight space.
**Why human:** Overflow only manifests at render time.

### Gaps Summary

No gaps. All three must-have truths verified against actual code; both artifacts exist, are substantive, and are wired; both key links WIRED; all three requirement IDs satisfied; no blocker anti-patterns; `flutter analyze` clean of errors; commits confirm diff limited to the two intended UI files. Only human visual/runtime QA remains, which is expected for a pure-UI compaction phase.

---

_Verified: 2026-09-17T11:00:00Z_
_Verifier: Claude (gsd-verifier)_
