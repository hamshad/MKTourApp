---
phase: 11.1-booking-screen-bottom-sheet-ui-compaction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/booking/ride_confirmation_screen.dart
  - lib/features/booking/confirm_booking_screen.dart
autonomous: true
requirements:
  - UI-11.1-01
  - UI-11.1-02
  - UI-11.1-03

must_haves:
  truths:
    - "Booking screen bottom section is a single compact ride-hail-style card — fare row, one tappable Online/Cash payment row, Confirm CTA with price + compact Schedule affordance — with the map visible above"
    - "Online (payment_link) still opens the payment WebView immediately at booking; Cash still goes straight to driver-matching"
    - "No API, payload, routing, or payment-logic change: booking POST still carries mandatory paymentMethod exactly as Phase 11 built it"
  artifacts:
    - path: "lib/features/booking/ride_confirmation_screen.dart"
      provides: "Compact bottom-sheet card on main booking screen"
      contains: "_buildBottomBar"
    - path: "lib/features/booking/confirm_booking_screen.dart"
      provides: "Visual-parity compaction on legacy confirm path"
      contains: "_buildPaymentMethodSelector"
  key_links:
    - from: "lib/features/booking/ride_confirmation_screen.dart"
      to: "PaymentWebViewScreen"
      via: "_processBooking link branch pushes PaymentWebViewScreen on success-gated flow"
      pattern: "PaymentWebViewScreen"
    - from: "lib/features/booking/ride_confirmation_screen.dart"
      to: "bookRideWithPayment"
      via: "_processBooking passes _selectedPaymentMethod as required paymentMethod"
      pattern: "paymentMethod: _selectedPaymentMethod|paymentMethod: paymentMethod"
---

<objective>
Redesign the ride booking bottom section into a compact bottom-sheet card so the map stays visible and the layout looks clean, without touching booking behavior.

Purpose: Phase 11's mandatory payment toggle made the bottom section too tall; pure UI compaction restores map visibility.
Output: Compact bottom bar on both booking screens, booking contract byte-identical.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-upfront-payments/11-02-SUMMARY.md
@lib/features/booking/ride_confirmation_screen.dart
@lib/features/booking/confirm_booking_screen.dart
@lib/features/booking/widgets/schedule_ride_sheet.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact bottom-sheet card on RideConfirmationScreen</name>
  <files>lib/features/booking/ride_confirmation_screen.dart</files>
  <action>Rework ONLY `_buildBottomBar` (+ `_buildPaymentMethodSelector` / `_buildPaymentMethodOption` presentation if needed) into a single compact bottom-sheet card inspired by Uber/Lyft/Bolt booking sheets. Target structure, top to bottom: (1) drag handle + one fare row (category/ETA left, price right — keep promo FREE/discount rendering; fold the multi-line fare-variance note into a tooltip/help icon or single-line caption); (2) ONE payment-method row in ride-hail style — a single tappable row showing method icon + current choice label (Online / Cash) + chevron, which expands inline (or opens a compact sheet) to reveal the Online (Card via Link) / Cash options — replacing the always-visible two-button toggle block; the row writes the existing `_selectedPaymentMethod` state; (3) ONE full-width primary Confirm CTA with price in the label (Uber-style `Confirm • £X`) plus Schedule/Prebook as a small text-button / icon-button beside or above the CTA, NOT a second full-height 54px button. Card styling: rounded top corners, sticky bottom with SafeArea, map visible above. Reference points: Uber (ride list + payment row + Choose CTA), Lyft (payment row + schedule affordance + upfront-price CTA), Bolt (compact card + segmented cash/card + confirm). Keep error branch (retry) and `_isLoading` / `_isFetchingFare` gating identical. DO NOT touch `_confirmRide`, `_confirmPrebook`, `_processBooking`, `_showScheduleSheet`, `_handleScheduledPayment`, `_switchScheduledPayment`, or any navigation/payload code. Handle small screens: bottom card must use flexible/min sizes and never overflow (test mentally at 320px width — use Flexible/FittedBox already in file, SingleChildScrollView only if the card itself could overflow).</action>
  <verify>
    <automated>flutter analyze lib/features/booking/ride_confirmation_screen.dart</automated>
    <manual>Bottom card shows fare row + single payment row + Confirm CTA in roughly half the previous vertical space; map visible above; expanding payment row reveals Online/Cash</manual>
  </verify>
  <done>Bottom section is one compact card; toggle still writes _selectedPaymentMethod; Confirm/Schedule call the same handlers; analyze clean</done>
</task>

<task type="auto">
  <name>Task 2: Parity compaction on legacy ConfirmBookingScreen</name>
  <files>lib/features/booking/confirm_booking_screen.dart</files>
  <action>Apply the same ride-hail compact treatment to the legacy path: collapse the always-visible toggle into the single payment-method row idiom from Task 1 (icon + label + chevron, expands to Online/Cash), tighten `_buildPaymentMethodSelector` / `_buildPaymentMethodOption` padding, and compact the Schedule + Confirm Booking action row into one full-width Confirm CTA (price in label) with Schedule as a small text-button (reduce 56px heights / 24px paddings, keep SafeArea). Visual parity with Task 1, smaller diff acceptable. DO NOT touch `_confirmBooking`, `_handleScheduleRide`, `_handleScheduledPayment`, `_switchScheduledPayment`, `_showScheduleSheet`, success/error dialogs, or the notes/stops sections. Keep the dialog-based success flow exactly as-is.</action>
  <verify>
    <automated>flutter analyze lib/features/booking/confirm_booking_screen.dart</automated>
    <manual>Legacy screen bottom section visibly shorter; toggle + both buttons still work the same</manual>
  </verify>
  <done>Legacy screen compacted with parity; no logic/routing change; analyze clean</done>
</task>

<task type="auto">
  <name>Task 3: Verify booking contract intact, no behavior/API change</name>
  <files>lib/features/booking/ride_confirmation_screen.dart, lib/features/booking/confirm_booking_screen.dart</files>
  <action>Read-only verification, no edits unless it catches a regression from Tasks 1-2 (fix forward if so): (1) `rg "paymentMethod" lib/features/booking/ride_confirmation_screen.dart lib/features/booking/confirm_booking_screen.dart` — both screens still pass a mandatory paymentMethod into bookRideWithPayment; (2) `rg "PaymentWebViewScreen" lib/features/booking/` — link branch still pushes WebView immediately, cash path has no WebView; (3) `git diff --stat` — only the two booking screen files changed (no api_service, payment_service, schedule sheet, or routing files); (4) `flutter analyze` on both files clean. If any check fails, repair the regression before closing.</action>
  <verify>
    <automated>flutter analyze lib/features/booking/ride_confirmation_screen.dart lib/features/booking/confirm_booking_screen.dart && rg "paymentMethod" lib/features/booking/ride_confirmation_screen.dart lib/features/booking/confirm_booking_screen.dart</automated>
    <manual>git diff --stat shows only the two intended files</manual>
  </verify>
  <done>Contract verified: mandatory paymentMethod present, link→WebView / cash→matching intact, diff limited to two UI files, analyze clean</done>
</task>

</tasks>

<verification>
flutter analyze on both booking screens clean; rg confirms paymentMethod + PaymentWebViewScreen wiring untouched; diff limited to two files; compact card renders without overflow on small screens.
</verification>

<success_criteria>
Bottom section is a single compact card with map visible above; Online/Cash toggle + immediate link/cash routing work exactly as in Phase 11; zero API/payload/routing/payment-logic changes.
</success_criteria>

<output>
After completion, create `.planning/phases/11.1-booking-screen-bottom-sheet-ui-compaction/11.1-01-SUMMARY.md`
</output>
