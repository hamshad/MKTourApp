---
phase: 13-account-suspension-safeguard-on-startup-outstanding-balance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/models/outstanding_balance.dart
  - test/suspension_contract_test.dart
autonomous: true
requirements:
  - SUSPEND-01
  - SUSPEND-05
must_haves:
  truths:
    - "Startup GET /payments/balance envelope with accountSuspended:true + allowCash:false parses to isSuspended true"
    - "Envelope without suspension flags parses to isSuspended false (today's behavior preserved)"
    - "403 booking block path still returns PaymentResult.balanceBlocked on both booking screens"
  artifacts:
    - path: "lib/core/models/outstanding_balance.dart"
      provides: "accountSuspended/allowCash fields + isSuspended getter + tolerant flag parser"
      contains: "isSuspended"
    - path: "test/suspension_contract_test.dart"
      provides: "Flag-parsing contract tests pinning INTEGRATION-GUIDE.md §1 JSON"
      min_lines: 40
  key_links:
    - from: "test/suspension_contract_test.dart"
      to: "lib/core/models/outstanding_balance.dart"
      via: "fromBalanceEnvelope with brief's exact JSON"
      pattern: "fromBalanceEnvelope"
---

<objective>
Suspension-flag parsing foundation + 403 backstop verification.

Purpose: Every downstream home-gate task needs `isSuspended`; pin it with contract tests first, and prove the create-block backstop is intact so the home gate never needs booking-screen edits.
Output: Extended `OutstandingBalance` model + new contract test file, zero Phase 12 regressions.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/INTEGRATION-GUIDE.md
@.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/13-RESEARCH.md
@lib/core/models/outstanding_balance.dart
@test/outstanding_balance_test.dart
@lib/core/services/payment_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend OutstandingBalance with suspension flags</name>
  <files>lib/core/models/outstanding_balance.dart</files>
  <action>Additive-only change (no existing field/factory signature changes): add `final bool accountSuspended` (default false) and `final bool allowCash` (default true) constructor params plus `bool get isSuspended => accountSuspended && !allowCash`. Add a private static tolerant `_flag(dynamic v, {required bool fallback})` helper (bool passthrough; num != 0; case-insensitive 'true'/'1'/'false'/'0' strings; else fallback) mirroring the existing `_num` precedent. Wire into `fromBalanceEnvelope` (accountSuspended fallback false, allowCash fallback true) and `fromBalanceDueEvent` (same fallbacks, for socket/FCM consistency). Also thread the two flags through the `succeeded` early-return branches (defaults) so every constructor call compiles. Do NOT touch `fromSelectMethodEnvelope` (live in-car flow, cash allowed) or `fromForbiddenEnvelope` (403 shape has neither flag). Do NOT touch excess_settlement_sheet.dart, ride_complete_screen.dart, driver_home_screen.dart, payment_service.dart, socket_service.dart, or any booking screen.</action>
  <verify><automated>flutter analyze lib/core/models/outstanding_balance.dart</automated></verify>
  <done>Model exposes isSuspended; absent flags default to not-suspended; analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Contract tests for flags + 403 backstop verification</name>
  <files>test/suspension_contract_test.dart</files>
  <action>Create `test/suspension_contract_test.dart` mirroring `test/outstanding_balance_test.dart` style. Cases: (1) brief's exact §1 JSON (rideId/excessAmount/paymentUrl/allowCash:false/accountSuspended:true/status balance_due) → isSuspended true, isOwed true, amount 2.50, hasPaymentUrl true; (2) same JSON minus both flags → isSuspended false (backward compat); (3) accountSuspended:true + allowCash:true → isSuspended false (conjunction gate, Pitfall 5); (4) string/int variants ('true'/1 vs 'false'/0) parse tolerantly; (5) fromBalanceDueEvent with flags → isSuspended true; (6) succeeded envelope → isPaid true, isSuspended false. Then verify SUSPEND-05 with NO edits: confirm `PaymentService.bookRideWithPayment` still returns `PaymentResult.balanceBlocked` on 403 (payment_service.dart:299-314), both booking screens still call `_openBalanceBlocked` on `isBalanceBlocked` (ride_confirmation_screen + confirm_booking_screen), and `error_display_helper.dart` still has the 'Clear balance to continue'/'Pay now' entry — record file:line evidence in the SUMMARY. Run the full test suite to prove no Phase 12 regression.</action>
  <verify><automated>flutter test test/suspension_contract_test.dart && flutter test && flutter analyze lib test</automated></verify>
  <done>All new flag tests pass, full suite green, analyze clean, 403 backstop evidence recorded with no behavior change.</done>
</task>

</tasks>

<verification>
`flutter test` fully green; `flutter analyze lib test` clean; `rg 'isSuspended' lib/core/models/outstanding_balance.dart` shows getter + two parse sites; `rg 'fromSelectMethodEnvelope|fromForbiddenEnvelope'` diff empty (untouched); SUMMARY lists 403 backstop file:line evidence.
</verification>

<success_criteria>
- SUSPEND-01 parsed tolerantly (bool/int/string) with safe defaults, pinned by contract tests on the brief's exact JSON.
- SUSPEND-05 verified unchanged (no booking-screen, payment_service, or mapper edits).
- Zero Phase 12 regressions (full test suite + analyze green).
</success_criteria>

<output>
After completion, create `.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/13-01-SUMMARY.md`
</output>
