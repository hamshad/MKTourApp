---
phase: 13-account-suspension-safeguard-on-startup-outstanding-balance
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - lib/features/home/home_screen.dart
autonomous: true
requirements:
  - SUSPEND-02
  - SUSPEND-03
  - SUSPEND-04
must_haves:
  truths:
    - "Suspended user cannot start a booking from home (search bar, airport tile, scheduled tile locked or re-show modal)"
    - "Suspended user sees a non-dismissible 'Account Temporarily Suspended' modal with amount and a single Pay-Online CTA (no cash option)"
    - "Tapping Pay opens OutstandingBalanceScreen WebView flow; success clears banner, unlocks entries, dismisses modal"
    - "payment:succeeded unlocks only after authoritative re-fetch shows balance cleared (no unlock on unrelated captures)"
  artifacts:
    - path: "lib/features/home/home_screen.dart"
      provides: "Suspension flag + entry locks + single-shot modal + unlock wiring"
      contains: "_isSuspended"
  key_links:
    - from: "lib/features/home/home_screen.dart"
      to: "lib/core/models/outstanding_balance.dart"
      via: "parsed.isSuspended sets _isSuspended"
      pattern: "isSuspended"
    - from: "lib/features/home/home_screen.dart"
      to: "lib/features/ride/outstanding_balance_screen.dart"
      via: "modal CTA pushes OutstandingBalanceScreen, result clears balance"
      pattern: "OutstandingBalanceScreen\(balance"
---

<objective>
Home startup suspension gate — entry lock, pay-online modal, unlock.

Purpose: Enforce INTEGRATION-GUIDE.md §1 on the rider home screen: suspended accounts can't enter booking and must pay online to restore access, reusing the existing balance pipeline (no new backend calls, no new socket events).
Output: Home-only gate in `home_screen.dart`; Phase 11/12 files untouched.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/INTEGRATION-GUIDE.md
@.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/13-RESEARCH.md
@lib/features/home/home_screen.dart
@lib/core/models/outstanding_balance.dart
@lib/features/ride/outstanding_balance_screen.dart
@.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suspension state + entry-point lock</name>
  <files>lib/features/home/home_screen.dart</files>
  <action>Add `_isSuspended = false` and `_suspensionModalOpen = false` to `_HomeScreenState` (next to `_pendingBalance`, ~line 73). Set `_isSuspended = parsed.isSuspended` wherever the global check resolves: alongside `_persistPendingBalance(parsed)` in `_checkPendingBalance`, in `_persistPendingBalance` itself (derive from the passed balance so socket/FCM paths ride along), and reset to false in `_clearPendingBalance`. Guard the three home booking entries — search-bar GestureDetector onTap (~:1754), airport-tile GestureDetector onTap (~:1811), scheduled-rides InkWell onTap (~:2010) — with `if (_isSuspended) { _showSuspensionModal(); return; }` as the first statement, preserving all existing navigation code below. Keep the existing orange banner as-is (it already says "Tap to pay and unlock booking"). Do NOT add gates to ride_confirmation_screen / confirm_booking_screen (403 backstop covers them). Do NOT change persistence keys.</action>
  <verify><automated>flutter analyze lib/features/home/home_screen.dart</automated></verify>
  <done>While suspended, all three home entries re-show the modal instead of navigating; unsuspended flow byte-identical; analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Single-shot non-dismissible suspension modal</name>
  <files>lib/features/home/home_screen.dart</files>
  <action>Add `_showSuspensionModal()` + `_maybeShowSuspensionModal()` (single show site enforcing the guard) following the `_handleRideExpiration` AlertDialog precedent (barrierDismissible:false, ~:974). Title 'Account Temporarily Suspended'; body: amount (£X.XX from `_pendingBalance`), backend message, plus 'Cash is not available after leaving the vehicle.' copy; single CTA `Pay £X Online Now` → `Navigator.push(OutstandingBalanceScreen(balance: ...))` then on return: success map → `_clearPendingBalance()` (+ dismiss, Task 3); else `_checkPendingBalance()` re-verify (exact `_openPendingBalance` pattern, :1279-1294). Guard against stacking/over-payment-screen nag (Pitfalls 1+6): `_suspensionModalOpen` flag, `RideEventDedupe`-style single-show-site, show only when home route is current and no balance/payment route is on top; post with runAfterFrame from socket/FCM/resume paths. Online-only: no cash button anywhere on this path (12-02 `rg 'cash'` audit must stay clean on outstanding_balance_screen.dart). Call `_maybeShowSuspensionModal()` from `_checkPendingBalance` after persist, from the `payment:balanceDue` socket handler after persist, and from `_setPendingBalanceFromFcm` (all funnel to the one guarded show site).</action>
  <verify><automated>flutter analyze lib/features/home/home_screen.dart && rg -n "cash" lib/features/ride/outstanding_balance_screen.dart | head</automated></verify>
  <done>Modal auto-shows once per suspension episode, never stacks or covers the payment screen, CTA routes to the existing online-only balance screen, no cash UI added.</done>
</task>

<task type="auto">
  <name>Task 3: Unlock on succeeded + clear re-fetch with modal dismiss</name>
  <files>lib/features/home/home_screen.dart</files>
  <action>Extend the unlock paths (SUSPEND-04, 12-02 pop-only-when-cleared discipline): in `_clearPendingBalance` also set `_isSuspended = false` and pop the suspension dialog if open (track dialog context like 12-03 `_excessCashDialogOpen` precedent; pop only if open, silently, no extra toast — the balance screen already toasts). Harden the `payment:succeeded` socket handler (:1272): on succeeded → authoritative `getGlobalPaymentBalance` re-fetch first → unlock (`_clearPendingBalance`) only when `data == null` or `status == succeeded`; otherwise refresh `_pendingBalance`/amount and stay locked (Pitfall 2 — succeeded also fires for unrelated mid-trip captures). Resume path (`didChangeAppLifecycleState` → `_checkPendingBalance`) flows through the same clear method, no separate unlock path. Verify full suite + analyze green and Phase 12 files untouched (`git status --porcelain` shows only home_screen.dart modified).</action>
  <verify><automated>flutter test && flutter analyze lib/features/home/home_screen.dart && git status --porcelain</automated></verify>
  <done>Payment clears banner + flag + modal (socket and resume paths); unrelated succeeded events never unlock; only home_screen.dart modified.</done>
</task>

</tasks>

<verification>
`flutter test` green; `flutter analyze` clean on home_screen.dart; `git status --porcelain` lists only `lib/features/home/home_screen.dart` (plus plan files); `rg -n '_isSuspended|_showSuspensionModal|_suspensionModalOpen' lib/features/home/home_screen.dart` hits state + 3 entry guards + show site + unlock; `rg -in 'cash' lib/features/ride/outstanding_balance_screen.dart` shows no cash button added.
</verification>

<success_criteria>
- SUSPEND-02: search bar, airport tile, scheduled tile all locked while suspended (tap re-shows modal).
- SUSPEND-03: non-dismissible modal with amount + Pay-Online-only CTA → existing OutstandingBalanceScreen WebView; no stacking, no resume nag.
- SUSPEND-04: unlock via succeeded + clear re-fetch (authoritative, no wrong-event unlock), modal dismissed silently.
- Zero edits outside home_screen.dart; Phase 11/12 behavior intact.
</success_criteria>

<output>
After completion, create `.planning/phases/13-account-suspension-safeguard-on-startup-outstanding-balance/13-02-SUMMARY.md`
</output>
