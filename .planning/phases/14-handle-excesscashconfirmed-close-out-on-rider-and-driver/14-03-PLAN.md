---
phase: 14-handle-excesscashconfirmed-close-out-on-rider-and-driver
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/features/driver/driver_home_screen.dart
autonomous: true
requirements:
  - CONFIRM-03
  - CONFIRM-04
must_haves:
  truths:
    - "Driver Collect-Cash modal closes when the confirm event arrives"
    - "Driver sees exactly one 'Cash excess payment confirmed!' toast"
    - "Listener survives socket reconnects"
  artifacts:
    - path: "lib/features/driver/driver_home_screen.dart"
      provides: "confirmed listener + modal close + toast"
      contains: "onExcessCashConfirmed"
  key_links:
    - from: "lib/features/driver/driver_home_screen.dart"
      to: "lib/core/services/socket_service.dart"
      via: "on/offExcessCashConfirmed inside _setupSocketListeners + dispose"
      pattern: "onExcessCashConfirmed|offExcessCashConfirmed"
    - from: "lib/features/driver/driver_home_screen.dart"
      to: "Collect-Cash modal"
      via: "_closeExcessCashDialogIfOpen then single toast"
      pattern: "_closeExcessCashDialogIfOpen"
---

<objective>
Driver close-out: on `payment:excessCashConfirmed`, close the Collect-Cash
modal (if open) and show one "Cash excess payment confirmed!" toast,
wired to survive socket reconnects.

Purpose: CONFIRM-03 driver close+toast + driver half of CONFIRM-04
(exactly-one toast even if `payment:succeeded` also fires).
Output: confirmed handler in driver home screen with dispose/reconnect
off-symmetry.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/INTEGRATION-GUIDE.md
@.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/14-RESEARCH.md
@lib/features/driver/driver_home_screen.dart
@lib/core/services/socket_service.dart
@lib/core/services/ride_event_dedupe.dart
</context>

<tasks>

<task type="auto">
  <name>Wire driver confirmed handler with modal close and single toast</name>
  <files>lib/features/driver/driver_home_screen.dart</files>
  <action>In lib/features/driver/driver_home_screen.dart, add the confirmed
listener inside _setupSocketListeners immediately after the
onExcessCashCancelled block (ends line ~1442), combining the cancelled
dedupe shape with _closeExcessCashDialogIfOpen (lines 2659-2667):
1. `_socketService.onExcessCashConfirmed((data) { if (!mounted) return;
   normalize to Map; RideEventDedupe.shouldHandleEvent(source:'socket',
   type:'payment_excess_cash_confirmed', data:map) else return;
   _closeExcessCashDialogIfOpen(); toast once via CustomSnackbar.success
   'Cash excess payment confirmed!'; })`.
2. Toast-once guard (CONFIRM-04): gate the toast on a small confirm-settled
   flag (e.g. per-ride confirmed set or boolean reset when a new excess
   request arrives) — NOT only on _excessCashDialogOpen, since the modal
   may already be closed by a co-fired succeeded event yet the toast must
   still fire exactly once.
3. NO `_currentRideId` gate — event rideId is authoritative (no-modal fix);
   optionally ignore only when both event rideId and _currentRideId are
   non-null and differ (mirror the cancelled handler lines 1435-1440).
4. Register `offExcessCashConfirmed()` in BOTH the dispose block
   (next to offExcessCashRequested/offExcessCashCancelled, lines ~588-589)
   AND the _setupSocketListeners reconnect preamble (lines ~1279-1280).
Rules: raw event string forbidden here; no AudioService ring; do NOT
alter the payment:succeeded reset-to-online flow (its _currentRideId
gate already excludes the excess flow — leave it); do NOT touch
selectPaymentMethod or arrival code.</action>
  <verify>
    <automated>flutter analyze lib/features/driver/driver_home_screen.dart</automated>
    <manual>Code read: handler sits inside _setupSocketListeners, no _currentRideId gate, off in both dispose and preamble</manual>
  </verify>
  <done>Modal closes silently then exactly one toast fires; reconnect re-arms the listener</done>
</task>

<task type="auto">
  <name>Verify driver close-out with tests and analyze</name>
  <files>lib/features/driver/driver_home_screen.dart</files>
  <action>Run flutter test on both excess-cash suites (plan 01 suite +
existing transport suite) to prove the shared dedupe contract still
holds, then flutter analyze on driver_home_screen.dart with baseline
comparison (`git stash` + analyze per 14-RESEARCH.md Pitfall 5).
Confirm zero new select-payment references and that the existing
payment:succeeded handler is byte-identical (git diff shows only the
confirmed block + two off lines). Record device-QA checklist in the
SUMMARY: one cash-confirm → modal closes + single toast; background/
resume → listener still fires; socket log checked for succeeded co-fire.</action>
  <verify>
    <automated>flutter test test/excess_cash_confirmed_closeout_test.dart test/excess_cash_modal_transport_test.dart</automated>
    <manual>Analyze baseline-clean; git diff limited to confirmed block + off lines; QA checklist in SUMMARY</manual>
  </verify>
  <done>Tests green, analyze baseline-clean, succeeded handler untouched</done>
</task>

</tasks>

<verification>
flutter test on both excess-cash suites green; flutter analyze shows no
new issues on driver_home_screen.dart vs baseline; rg confirms raw event
string absent from the screen file.
</verification>

<success_criteria>
Driver confirm round-trip closes the modal with exactly one success
toast on the confirming device; listener survives reconnect; online-pay
succeeded flow behavior-identical (no double toast, no reset change).
</success_criteria>

<output>
After completion, create `.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/14-03-SUMMARY.md`
</output>
