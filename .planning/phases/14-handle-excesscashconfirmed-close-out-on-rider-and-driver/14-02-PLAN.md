---
phase: 14-handle-excesscashconfirmed-close-out-on-rider-and-driver
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/features/ride/excess_settlement_sheet.dart
autonomous: true
requirements:
  - CONFIRM-02
  - CONFIRM-04
must_haves:
  truths:
    - "Rider waiting on cash sees settled confirmation when driver confirms"
    - "Rider sheet pops only after authoritative balance proof, never on the event alone"
    - "A co-fired payment:succeeded cannot double-pop the sheet"
  artifacts:
    - path: "lib/features/ride/excess_settlement_sheet.dart"
      provides: "confirmed listener + waiting-state exit"
      contains: "_onExcessCashConfirmed"
  key_links:
    - from: "lib/features/ride/excess_settlement_sheet.dart"
      to: "lib/core/services/socket_service.dart"
      via: "on/offExcessCashConfirmed passthrough"
      pattern: "onExcessCashConfirmed|offExcessCashConfirmed"
    - from: "lib/features/ride/excess_settlement_sheet.dart"
      to: "authoritative balance re-fetch"
      via: "_refresh(fromEvent: true) shared with succeeded handler"
      pattern: "_refresh\\(fromEvent: true\\)"
---

<objective>
Rider close-out: on `payment:excessCashConfirmed`, the
`ExcessSettlementSheet` waitingCash state exits with settled confirmation
via the existing authoritative `_refresh(fromEvent:true)` path.

Purpose: CONFIRM-02 rider exit + rider half of CONFIRM-04 (shared
`_settled` guard makes a co-fired `payment:succeeded` a no-op).
Output: confirmed listener wired in the rider settlement sheet.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/INTEGRATION-GUIDE.md
@.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/14-RESEARCH.md
@lib/features/ride/excess_settlement_sheet.dart
@lib/core/services/socket_service.dart
@lib/core/services/ride_event_dedupe.dart
</context>

<tasks>

<task type="auto">
  <name>Wire confirmed listener with tolerant settled close-out</name>
  <files>lib/features/ride/excess_settlement_sheet.dart</files>
  <action>In lib/features/ride/excess_settlement_sheet.dart, mirror
_onPaymentSucceeded (lines 56-72) with _onExcessCashConfirmed:
1. initState: add `_socketService.onExcessCashConfirmed(_onExcessCashConfirmed)`
   next to the payment:succeeded registration; dispose: add matching
   `offExcessCashConfirmed` (off-symmetry).
2. Handler: normalize data to Map, rideId-match guard
   (`id != widget.rideId → return`, copying line 59 with bookingId/_id
   fallbacks), then `RideEventDedupe.shouldHandleEvent(source:'socket',
   type:'payment_excess_cash_confirmed', data:map)`, then
   `if (!mounted || _settled) return`, then `_refresh(fromEvent:true)`.
3. Thank-you copy: prefer the event's non-empty `message` field
   ("Driver confirmed cash receipt ... Thank you!") for the success
   snackbar, fallback to existing "Excess paid successfully!" copy —
   implement as an optional message passthrough into _refresh or a
   pre-set field, NOT a second pop path. Display verbatim, never
   substring-match.
Rules: never pop on the event alone (404-fix lesson — mid-trip captures
also fire succeeded); reuse _settled at handler entry (CONFIRM-04);
raw `payment:excessCashConfirmed` string forbidden here — passthrough
only. No AudioService ring (toast suffices). Leave _selectCash,
WebView, and OutstandingBalanceScreen untouched.</action>
  <verify>
    <automated>flutter analyze lib/features/ride/excess_settlement_sheet.dart</automated>
    <manual>Code read: handler order is rideId-match → dedupe → settled → refresh; dispose has off-symmetry</manual>
  </verify>
  <done>WaitingCash exits via authoritative refresh on confirmed; succeeded co-fire hits settled guard and no-ops</done>
</task>

<task type="auto">
  <name>Verify rider close-out with tests and analyze</name>
  <files>lib/features/ride/excess_settlement_sheet.dart</files>
  <action>Run flutter test on the confirmed close-out suite from plan 01
plus the existing transport suite to prove no regression, then
flutter analyze on the sheet with baseline comparison
(`git stash` + analyze per 14-RESEARCH.md Pitfall 5 — pre-existing
use_build_context_synchronously infos ignored). Confirm zero new
`select-payment` references (rg select-payment in the sheet). Record
the open live-backend question in the SUMMARY: whether backend ALSO
emits payment:succeeded for driver-cash-confirm (device QA should log
both events in one cash-confirm; code handles either order).</action>
  <verify>
    <automated>flutter test test/excess_cash_confirmed_closeout_test.dart test/excess_cash_modal_transport_test.dart</automated>
    <manual>Analyze shows zero new issues vs stashed baseline; device-QA note recorded in SUMMARY</manual>
  </verify>
  <done>Tests green, analyze baseline-clean, no select-payment regressions</done>
</task>

</tasks>

<verification>
flutter test on both excess-cash suites green; flutter analyze shows no
new issues on the sheet vs baseline; rg confirms raw event string absent
from the sheet.
</verification>

<success_criteria>
Rider in waitingCash sees thank-you settled confirmation and exits to
receipt/rating on driver confirm; a second succeeded event cannot
double-pop (settled guard); still-owed refresh keeps the sheet open.
</success_criteria>

<output>
After completion, create `.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/14-02-SUMMARY.md`
</output>
