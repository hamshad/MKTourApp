---
phase: 14-handle-excesscashconfirmed-close-out-on-rider-and-driver
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/services/socket_service.dart
  - test/excess_cash_confirmed_closeout_test.dart
autonomous: true
requirements:
  - CONFIRM-01
  - CONFIRM-04
must_haves:
  truths:
    - "Socket event payment:excessCashConfirmed reaches screens through a typed passthrough"
    - "Duplicate confirmed deliveries for the same ride are handled exactly once"
    - "Confirmed dedupe key never collides with requested/cancelled/succeeded keys"
  artifacts:
    - path: "lib/core/services/socket_service.dart"
      provides: "on/offExcessCashConfirmed passthroughs"
      contains: "onExcessCashConfirmed"
    - path: "test/excess_cash_confirmed_closeout_test.dart"
      provides: "confirmed close-out regression tests"
      min_lines: 40
  key_links:
    - from: "lib/core/services/socket_service.dart"
      to: "payment:excessCashConfirmed"
      via: "on/off passthrough pair"
      pattern: "payment:excessCashConfirmed"
---

<objective>
Socket transport foundation for the driver-cash close-out: typed
`on/offExcessCashConfirmed` passthroughs plus dedupe regression tests.
Later plans (rider sheet, driver modal) consume these passthroughs.

Purpose: CONFIRM-01 passthrough+dedupe contract and the CONFIRM-04 test
half (exactly-once, no key collision) — no screen behavior yet.
Output: passthrough pair in SocketService, green regression test file.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/INTEGRATION-GUIDE.md
@.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/14-RESEARCH.md
@lib/core/services/socket_service.dart
@lib/core/services/ride_event_dedupe.dart
@test/excess_cash_modal_transport_test.dart
</context>

<tasks>

<task type="auto">
  <name>Add on/offExcessCashConfirmed passthroughs</name>
  <files>lib/core/services/socket_service.dart</files>
  <action>In lib/core/services/socket_service.dart, immediately after the
offExcessCashCancelled pair (lines ~706-708), add the confirmed pair
mirroring the requested/cancelled shape exactly:

/// Driver confirmed cash receipt (Phase 14 INTEGRATION-GUIDE.md §2).
/// Payload: {rideId, excessAmount, message}.
void onExcessCashConfirmed(void Function(dynamic) handler) {
  on('payment:excessCashConfirmed', handler);
}

void offExcessCashConfirmed() {
  off('payment:excessCashConfirmed');
}

Rules: exact colon-camelCase string `payment:excessCashConfirmed` only —
no snake_case variants. Raw string lives ONLY here (STATE.md [12-03]
decision); screens must call the passthroughs, never the raw string.
Do NOT gate on rideId, do NOT toast, do NOT touch selectPaymentMethod
(deprecated) or OutstandingBalanceScreen.</action>
  <verify>
    <automated>flutter analyze lib/core/services/socket_service.dart</automated>
    <manual>grep confirms exactly one occurrence of the raw string, inside socket_service.dart</manual>
  </verify>
  <done>on/offExcessCashConfirmed exist with off-symmetry; analyze shows no new issues vs baseline</done>
</task>

<task type="auto">
  <name>Create confirmed close-out regression tests</name>
  <files>test/excess_cash_confirmed_closeout_test.dart</files>
  <action>Create test/excess_cash_confirmed_closeout_test.dart extending the
test/excess_cash_modal_transport_test.dart pattern (RideEventDedupe +
setUp resetForTests). Cover with dedupe key
`payment_excess_cash_confirmed`:
1. socket confirmed twice for same ride → second shouldHandleEvent false
2. confirmed vs succeeded-settlement same ride → second false (no double close-out)
3. confirmed for different ride → true (still flows)
4. confirmed key distinct from requested/cancelled keys (no collision)
Use small maps like {'rideId': 'ride_confirm_1', 'excessAmount': 7.8}.
No widget tests, no new harness — pure dedupe contract.</action>
  <verify>
    <automated>flutter test test/excess_cash_confirmed_closeout_test.dart</automated>
    <manual>All new tests pass; existing transport tests still green</manual>
  </verify>
  <done>New test file green; confirmed-vs-succeeded double-delivery pinned as skip</done>
</task>

</tasks>

<verification>
flutter test test/excess_cash_confirmed_closeout_test.dart test/excess_cash_modal_transport_test.dart passes; flutter analyze clean on touched files (baseline-compare per 14-RESEARCH.md Pitfall 5 — pre-existing infos ignored).
</verification>

<success_criteria>
Screens can subscribe via on/offExcessCashConfirmed; duplicate confirmed
events for one ride collapse to exactly-once; no key collision with
requested/cancelled/succeeded.
</success_criteria>

<output>
After completion, create `.planning/phases/14-handle-excesscashconfirmed-close-out-on-rider-and-driver/14-01-SUMMARY.md`
</output>
