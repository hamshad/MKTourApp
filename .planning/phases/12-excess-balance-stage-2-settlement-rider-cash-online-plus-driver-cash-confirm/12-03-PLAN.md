---
phase: 12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - lib/features/driver/driver_home_screen.dart
  - lib/core/models/error_display_helper.dart
autonomous: true
requirements:
  - STAGE2-04
  - STAGE2-05
  - STAGE2-06
must_haves:
  truths:
    - "Driver receives Collect Cash modal with £X amount when rider requests cash"
    - "Confirm Cash Received calls confirm-driver-cash with driver token, closes modal, toasts success"
    - "Failed confirm keeps modal open with inline error"
    - "Rider switching to online auto-closes driver modal silently"
  artifacts:
    - path: "lib/features/driver/driver_home_screen.dart"
      provides: "excess cash modal + confirm wiring + auto-close"
      contains: "_excessCashDialogOpen"
    - path: "lib/core/models/error_display_helper.dart"
      provides: "confirm-driver-cash error copy in central mapper"
      contains: "confirmDriverCash"
  key_links:
    - from: "lib/features/driver/driver_home_screen.dart"
      to: "lib/core/services/socket_service.dart"
      via: "on/offExcessCashRequested + on/offExcessCashCancelled passthroughs"
      pattern: "onExcessCash(Requested|Cancelled)"
    - from: "lib/features/driver/driver_home_screen.dart"
      to: "lib/core/api_service.dart"
      via: "confirmDriverCash on Confirm tap"
      pattern: "confirmDriverCash"
    - from: "payment:excessCashCancelled socket"
      to: "_excessCashDialogOpen modal"
      via: "pop only if flag true, silent no-toast"
      pattern: "_excessCashDialogOpen"
---

<objective>
Driver cash-confirm flow: `payment:excessCashRequested` raises Collect-Cash modal, Confirm calls `confirm-driver-cash`, `payment:excessCashCancelled` auto-closes it.

Purpose: STAGE2-04/05/06 — driver half of in-car settlement; same codebase serves both roles so modal lives in driver flow.
Output: Driver home screen modal + wiring + mapper copy.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/INTEGRATION-GUIDE.md
@.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/12-RESEARCH.md
@lib/features/driver/driver_home_screen.dart
@lib/core/services/socket_service.dart
@lib/core/services/ride_event_dedupe.dart
@lib/core/api_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cash-request modal + confirm-driver-cash wiring</name>
  <files>lib/features/driver/driver_home_screen.dart</files>
  <action>Register `onExcessCashRequested`/`onExcessCashCancelled` (+ `payment:succeeded` for same ride) in `_setupSocketListeners` with off-before-on hygiene, matching existing payment-listener block; add matching `off()` calls in `dispose` (reconnect re-setup flows through setup automatically). Requested handler: normalize payload to map, dedupe-guard `type:'payment_excess_cash_requested'`, rideId match against `_currentRideId` accepting `rideId/bookingId/_id/id` keys, amount via `excessAmount ?? amount`; open `barrierDismissible:false` AlertDialog via StatefulBuilder (match `_showCancellationReasonDialog` pattern): "Collect Cash: £X — Passenger requested to pay £X excess balance in cash." + Confirm Cash Received button with inline spinner; set `_excessCashDialogOpen=true` on open. Confirm tap → `confirmDriverCash(rideId)` → success: pop via dialog context + `CustomSnackbar` "Cash payment confirmed! Ride fully completed."; failure: inline `sheetError` via `RideErrorMapper`, modal STAYS open. `mounted` checks everywhere, never store outer BuildContext across awaits (pitfall 2).</action>
  <verify><automated>flutter analyze lib/features/driver/driver_home_screen.dart</automated><manual>Modal opens once per event (no stacking on duplicates), failure keeps modal open with inline error, success toasts</manual></verify>
  <done>Modal + confirm wired, dedupe + rideId match + failure-stays-open correct, analyze clean</done>
</task>

<task type="auto">
  <name>Task 2: Auto-close on cancelled/succeeded + mapper copy</name>
  <files>lib/features/driver/driver_home_screen.dart, lib/core/models/error_display_helper.dart</files>
  <action>Cancelled handler: dedupe-guard `type:'payment_excess_cash_cancelled'`, if `_excessCashDialogOpen==true` pop via stored dialog context and set flag false — silent, NO error toast (rider merely switched to online). Also pop on `payment:succeeded` for that rideId (settled elsewhere). Mapper: 12-02 runs first in Wave 2 and adds select-method cases to `RideErrorMapper` — READ the file first and APPEND confirm-driver-cash 400/401/403/409 cases alongside (do NOT rewrite file, preserve existing `selectBalanceMethod` entries). No raw status strings in UI; driver-role token assumed by device login per pitfall 5 — surface mapper copy on failure. Verify exact event strings `payment:excessCashRequested` / `payment:excessCashCancelled` (colon-camelCase, NOT snake_case — pitfall 6).</action>
  <verify><automated>flutter analyze lib/features/driver/driver_home_screen.dart lib/core/models/error_display_helper.dart && rg "payment:excessCash(Requested|Cancelled)" lib/features/driver/driver_home_screen.dart</automated><manual>Cancelled auto-closes open modal silently; no toast spam; existing payment:succeeded driver reset still intact</manual></verify>
  <done>Auto-close silent and flag-guarded, mapper covers confirm errors, exact event strings verified</done>
</task>

</tasks>

<verification>
`flutter analyze` clean; `rg 'excessCash' lib/` hits socket service + driver screen only; `rg 'payment_selected|select-payment'` shows no new old-event subscriptions; modal flag `_excessCashDialogOpen` set/cleared on every open/pop path.
</verification>

<success_criteria>
Cash request → one Collect-Cash modal with amount; Confirm → API → toast + close; failure → inline error, stays open; rider-online-switch → silent auto-close; no duplicate modals, no orphaned dialogs.
</success_criteria>

<output>
After completion, create `.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/12-03-SUMMARY.md`
</output>
