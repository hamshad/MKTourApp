---
phase: 12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/constants/api_constants.dart
  - lib/core/api_service.dart
  - lib/core/models/outstanding_balance.dart
  - lib/core/services/socket_service.dart
  - test/excess_settlement_contract_test.dart
autonomous: true
requirements:
  - STAGE2-02
  - STAGE2-05
must_haves:
  truths:
    - "select-method and confirm-driver-cash endpoints are callable from ApiService with never-throws envelope"
    - "select-method response parses paymentUrl and amount tolerantly across envelope shapes"
    - "driver socket events have symmetric on/off passthroughs for requested and cancelled"
  artifacts:
    - path: "lib/core/api_service.dart"
      provides: "selectBalanceMethod + confirmDriverCash via _postRequest"
      exports: ["selectBalanceMethod", "confirmDriverCash"]
    - path: "lib/core/constants/api_constants.dart"
      provides: "selectBalanceMethod + confirmDriverCash URL builders"
      contains: "select-method"
    - path: "lib/core/models/outstanding_balance.dart"
      provides: "fromSelectMethodEnvelope tolerant parser"
      contains: "fromSelectMethodEnvelope"
    - path: "lib/core/services/socket_service.dart"
      provides: "excess cash requested/cancelled passthroughs"
      exports: ["onExcessCashRequested", "offExcessCashRequested", "onExcessCashCancelled", "offExcessCashCancelled"]
    - path: "test/excess_settlement_contract_test.dart"
      provides: "select-method envelope parsing tests"
      min_lines: 40
  key_links:
    - from: "lib/core/api_service.dart"
      to: "lib/core/constants/api_constants.dart"
      via: "selectBalanceMethod/confirmDriverCash URL builders in _postRequest call"
      pattern: "ApiConstants\\.(selectBalanceMethod|confirmDriverCash)"
    - from: "test/excess_settlement_contract_test.dart"
      to: "lib/core/models/outstanding_balance.dart"
      via: "imports and exercises fromSelectMethodEnvelope"
      pattern: "fromSelectMethodEnvelope"
---

<objective>
API + socket + model foundation every later plan builds on: two new endpoint methods, two URL builders, tolerant select-method parser, four socket passthroughs, contract tests proving envelope tolerance.

Purpose: STAGE2-02 and STAGE2-05 transport must exist before any UI calls it; pins unverified live envelope shapes in tests per research open question 1.
Output: 4 source edits + 1 new test file, all green.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/INTEGRATION-GUIDE.md
@.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/12-RESEARCH.md
@lib/core/api_service.dart
@lib/core/constants/api_constants.dart
@lib/core/models/outstanding_balance.dart
@lib/core/services/socket_service.dart
@test/outstanding_balance_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint URLs + ApiService methods</name>
  <files>lib/core/constants/api_constants.dart, lib/core/api_service.dart</files>
  <action>Add `selectBalanceMethod(rideId)` → `$baseUrl/payments/balance/$rideId/select-method` and `confirmDriverCash(rideId)` → `$baseUrl/payments/balance/$rideId/confirm-driver-cash` next to existing `paymentBalance(rideId)` builder. Add `selectBalanceMethod(rideId, paymentMethod)` posting `{'paymentMethod': paymentMethod}` ('cash'|'payment_link') and `confirmDriverCash(rideId)` posting `{}` — both delegating to `_postRequest` (never throws, token injection + 401 hook inherited). Do NOT touch deprecated `selectPaymentMethod` (rides/:id/select-payment) or `getPaymentBalance`.</action>
  <verify><automated>flutter analyze lib/core/api_service.dart lib/core/constants/api_constants.dart</automated><manual>rg confirms `select-method` and `confirm-driver-cash` present, zero new `select-payment` references</manual></verify>
  <done>Both methods callable, analyze clean, rg shows new endpoints and no resurrected select-payment</done>
</task>

<task type="auto">
  <name>Task 2: Tolerant parser + socket passthroughs + contract tests</name>
  <files>lib/core/models/outstanding_balance.dart, lib/core/services/socket_service.dart, test/excess_settlement_contract_test.dart</files>
  <action>Model: add `OutstandingBalance.fromSelectMethodEnvelope(Map)` tolerating flat `paymentUrl` AND nested `data.payment.paymentUrl`, amount keys `excessAmount ?? outstandingBalance ?? amount`, plus optional status/success passthrough — mirror existing `fromBalanceEnvelope` tolerance, no new amount model. Socket: add `onExcessCashRequested/offExcessCashRequested` for exact string `payment:excessCashRequested` and `onExcessCashCancelled/offExcessCashCancelled` for `payment:excessCashCancelled`, mirroring `onPaymentBalanceDue` (colon form, NOT snake_case). Tests: create `test/excess_settlement_contract_test.dart` modelled on `test/outstanding_balance_test.dart` + `test/upfront_payment_contract_test.dart` covering flat paymentUrl, nested data.payment.paymentUrl, each amount key, cash-selection envelope without paymentUrl, and list-envelope shape; every case asserts amount + paymentUrl-or-null.</action>
  <verify><automated>flutter test test/excess_settlement_contract_test.dart && flutter analyze lib/core/models/outstanding_balance.dart lib/core/services/socket_service.dart</automated><manual>Socket passthroughs use exact colon-camelCase strings</manual></verify>
  <done>Parser handles all envelope shapes, 4 passthroughs exist with off-symmetry, contract tests green, analyze clean</done>
</task>

</tasks>

<verification>
`flutter test test/excess_settlement_contract_test.dart` green; `flutter analyze` clean on all 4 touched lib files; `rg 'select-method|confirm-driver-cash|excessCash(Requested|cancelled)'` hits only intended files.
</verification>

<success_criteria>
ApiService exposes both endpoints via _postRequest; parser tolerates flat + nested paymentUrl and 3 amount keys; socket passthroughs registered with exact event strings; no select-payment resurrection.
</success_criteria>

<output>
After completion, create `.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/12-01-SUMMARY.md`
</output>
