---
phase: 12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - lib/features/ride/excess_settlement_sheet.dart
  - lib/features/ride/ride_complete_screen.dart
  - lib/core/models/error_display_helper.dart
autonomous: true
requirements:
  - STAGE2-01
  - STAGE2-02
  - STAGE2-03
must_haves:
  truths:
    - "Rider on live trip-completion screen sees Cash and Pay Online options when excess is owed"
    - "Tapping Cash calls select-method with cash and shows driver-confirm waiting state with a way back to Online"
    - "Tapping Pay Online calls select-method with payment_link and opens paymentUrl in PaymentWebViewScreen"
    - "payment:succeeded closes settlement back to receipt/rating only after authoritative re-fetch confirms cleared"
    - "Startup OutstandingBalanceScreen stays online-only with no cash button"
  artifacts:
    - path: "lib/features/ride/excess_settlement_sheet.dart"
      provides: "Cash/Online buttons + waiting state + succeeded close-out"
      min_lines: 120
    - path: "lib/features/ride/ride_complete_screen.dart"
      provides: "Hosts settlement sheet when excess owed"
      contains: "ExcessSettlementSheet"
    - path: "lib/core/models/error_display_helper.dart"
      provides: "select-method error copy in central mapper"
      contains: "selectBalanceMethod"
  key_links:
    - from: "lib/features/ride/excess_settlement_sheet.dart"
      to: "lib/core/api_service.dart"
      via: "selectBalanceMethod cash/payment_link calls"
      pattern: "selectBalanceMethod"
    - from: "lib/features/ride/excess_settlement_sheet.dart"
      to: "PaymentWebViewScreen"
      via: "push paymentUrl, success re-fetch, cancel returns to choice"
      pattern: "PaymentWebViewScreen"
    - from: "lib/features/ride/excess_settlement_sheet.dart"
      to: "payment:succeeded socket"
      via: "rideId-match, authoritative GET, pop-only-when-cleared"
      pattern: "payment:succeeded"
---

<objective>
Rider live-settlement surface: new `ExcessSettlementSheet` (Cash + Online + waiting + WebView + succeeded close-out) hosted on `RideCompleteScreen`, settling on top of the receipt so rating stays intact.

Purpose: STAGE2-01/02/03 — in-car excess settlement the backend guide mandates; startup screen untouched per contract.
Output: 1 new widget + receipt wiring + mapper copy.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/INTEGRATION-GUIDE.md
@.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/12-RESEARCH.md
@lib/features/ride/ride_complete_screen.dart
@lib/features/ride/outstanding_balance_screen.dart
@lib/features/ride/payment_webview_screen.dart
@lib/features/ride/ride_assigned_screen.dart
@lib/core/models/outstanding_balance.dart
@lib/core/services/ride_event_dedupe.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: ExcessSettlementSheet widget with method selection + waiting + WebView</name>
  <files>lib/features/ride/excess_settlement_sheet.dart</files>
  <action>Create `ExcessSettlementSheet({required rideId, required OutstandingBalance balance})` bottom-sheet content: (a) method-choice state — "Pay Cash to Driver" + "Pay Online" buttons with amount headline; cash → `selectBalanceMethod(rideId,'cash')` → waiting state "Waiting for driver to confirm cash receipt…" with "Switch to Online" button (re-calls select-method with payment_link, per contract fires excessCashCancelled driver-side) + back chevron to method choice (NO dead-end spinner — pitfall 3); online → `selectBalanceMethod(rideId,'payment_link')` → parse paymentUrl via `fromSelectMethodEnvelope` (flat or data.payment.paymentUrl) → push `PaymentWebViewScreen(paymentUrl, rideId)` reusing existing contract: `{success:true}` → authoritative `getPaymentBalance` refresh, `{success:false}` → warning snackbar back to choice. Errors via `RideErrorMapper`/`ErrorDisplayHelper.showRideError`, never inline strings. Guard socket open path with `RideEventDedupe.shouldHandleEvent(source:'socket', type:'payment_succeeded_settlement', data: map)`.</action>
  <verify><automated>flutter analyze lib/features/ride/excess_settlement_sheet.dart</automated><manual>Waiting state exposes Switch-to-Online + back; online path pushes PaymentWebViewScreen; no cash UI on startup screen</manual></verify>
  <done>Sheet compiles; cash → waiting with escape hatches; online → WebView; errors via central mapper</done>
</task>

<task type="auto">
  <name>Task 2: Host on receipt + succeeded close-out + mapper copy</name>
  <files>lib/features/ride/ride_complete_screen.dart, lib/core/models/error_display_helper.dart</files>
  <action>Receipt: when completed ride carries excess owed (existing `pendingBalance` handoff / `payment:balanceDue` post-completion path, same `consumePendingBalance` precedent), present `ExcessSettlementSheet` ON TOP of the receipt summary (push/modal overlay — pop reveals receipt + rating intact, per research open-question-5 decision). Succeeded handler: copy `OutstandingBalanceScreen._onPaymentSucceeded` EXACTLY — rideId-match → authoritative `getPaymentBalance` re-fetch → pop settlement ONLY on `status==succeeded` or 404-after-event; if balance still owed, setState fresh amount and stay open (never pop on event alone — pitfall 1). Mapper: add select-method 400/403/409 cases to `RideErrorMapper` via targeted append (same style as Phase 11 upfront cases) — do NOT reformat or rewrite unrelated mapper entries; 12-03 Wave 3 appends confirm-driver-cash cases after this plan. Do NOT edit `outstanding_balance_screen.dart` (stays online-only) or `home_screen.dart` startup check.</action>
  <verify><automated>flutter analyze lib/features/ride/ride_complete_screen.dart lib/core/models/error_display_helper.dart && rg -l "Pay Cash" lib/features/ride/outstanding_balance_screen.dart; test $? -ne 0</automated><manual>Live receipt shows settlement when excess owed; succeeded pops only after cleared GET; startup screen has no cash button</manual></verify>
  <done>Receipt hosts sheet on top; succeeded re-fetch-and-pop correct; mapper covers select-method; startup untouched</done>
</task>

</tasks>

<verification>
`flutter analyze` clean on all touched files; `rg 'select-payment'` shows zero live arrival-flow calls; `rg 'Pay Cash|cash' lib/features/ride/outstanding_balance_screen.dart` empty (online-only preserved).
</verification>

<success_criteria>
Excess owed on live completion → Cash/Online sheet on top of receipt; cash waits with Switch-to-Online escape; online pays via WebView; succeeded closes only when GET confirms cleared; startup path unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/12-excess-balance-stage-2-settlement-rider-cash-online-plus-driver-cash-confirm/12-02-SUMMARY.md`
</output>
