---
phase: 15-outstanding-balance-silent-booking-fare-transparency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/features/booking/widgets/vehicle_selection_widget.dart, test/fare_estimate_balance_test.dart]
autonomous: true
requirements: [BAL-01, BAL-02]
user_setup: []

must_haves:
  truths:
    - "Fare-estimate categories carrying outstandingBalance parse without error"
    - "Parsed fare map exposes the balance separately while the displayed total stays exactly the backend estimatedFare"
    - "Categories without the new field behave exactly as before (balance defaults to 0)"
  artifacts:
    - path: "lib/features/booking/widgets/vehicle_selection_widget.dart"
      provides: "outstandingBalance parsing in _normalizeCategory + fare map key"
      contains: "outstanding_balance"
    - path: "test/fare_estimate_balance_test.dart"
      provides: "Contract tests pinning brief section 1 JSON behavior"
      min_lines: 40
  key_links:
    - from: "lib/features/booking/widgets/vehicle_selection_widget.dart"
      to: "fare-estimate category JSON"
      via: "_normalizeCategory tolerant parse"
      pattern: "outstandingBalance"
---

<objective>
Parse the new `outstandingBalance` fare-estimate field into the fare map without touching totals.

Purpose: Backend already adds the debt into `estimatedFare` — app must surface the split (plan 02 UI) without double-counting.
Output: Normalizer carries `outstanding_balance`, contract tests pin the brief §1 example.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-outstanding-balance-silent-booking-fare-transparency/INTEGRATION-GUIDE.md
@.planning/phases/15-outstanding-balance-silent-booking-fare-transparency/15-RESEARCH.md
@lib/features/booking/widgets/vehicle_selection_widget.dart
@lib/core/models/outstanding_balance.dart
@test/outstanding_balance_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carry outstandingBalance through _normalizeCategory</name>
  <files>lib/features/booking/widgets/vehicle_selection_widget.dart</files>
  <action>In `_normalizeCategory` (around line 258-299), parse tolerant `numOf(cat['outstandingBalance'] ?? cat['excessAmount'] ?? cat['outstanding_balance'] ?? 0)` clamped to >= 0, add `'outstanding_balance': value.toDouble()` to the returned `fare` map. Do NOT alter `total_fare`/`original_fare` computation — backend total is authoritative, no client-side addition. Follow the existing `numOf`/`congestionAmount` pattern in the same function.</action>
  <verify><automated>flutter analyze lib/features/booking/widgets/vehicle_selection_widget.dart</automated></verify>
  <done>Category JSON with `"outstandingBalance": 5, "estimatedFare": 15` yields fare map `{total_fare: 15.0, outstanding_balance: 5.0}`; missing field yields `outstanding_balance: 0.0`</done>
</task>

<task type="auto">
  <name>Task 2: Contract tests for balance parsing</name>
  <files>test/fare_estimate_balance_test.dart</files>
  <action>Create `test/fare_estimate_balance_test.dart` mirroring `test/outstanding_balance_test.dart` style: test brief §1 category (`estimatedFare 15, outstandingBalance 5`) parses to total 15.0 + balance 5.0 via the same tolerant expression; missing field → 0.0; string "5" → 5.0; verify test fails if balance is added into total (documents no-double-count rule). Test the parse rule directly (extract a pure helper or replicate the `numOf` expression) — do not instantiate the widget.</action>
  <verify><automated>flutter test test/fare_estimate_balance_test.dart</automated></verify>
  <done>4+ tests green covering brief example, missing-field default, string tolerance, no-double-count</done>
</task>

</tasks>

<verification>
`flutter analyze` clean on touched file; `flutter test test/fare_estimate_balance_test.dart` green.
</verification>

<success_criteria>
Fare map carries `outstanding_balance` per category, totals byte-identical to backend, tests pin the contract.
</success_criteria>

<output>
After completion, create `.planning/phases/15-outstanding-balance-silent-booking-fare-transparency/15-01-SUMMARY.md`
</output>
