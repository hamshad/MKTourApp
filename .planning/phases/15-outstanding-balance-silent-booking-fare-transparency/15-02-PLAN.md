---
phase: 15-outstanding-balance-silent-booking-fare-transparency
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [lib/features/booking/ride_confirmation_screen.dart, lib/features/booking/confirm_booking_screen.dart, lib/features/booking/widgets/vehicle_selection_widget.dart]
autonomous: true
requirements: [BAL-03, BAL-04]
user_setup: []

must_haves:
  truths:
    - "Rider selecting a category with unpaid balance sees an 'Includes £X.XX unpaid balance' line before confirming"
    - "Booking with unpaid balance succeeds via the normal create flow (no 403 dead-end for balance)"
    - "Stripe/payment-link amount covers the combined total with zero client-side fare math"
    - "Cancel/refund edge cases need no app logic (backend owns re-link/clear)"
  artifacts:
    - path: "lib/features/booking/ride_confirmation_screen.dart"
      provides: "Balance transparency banner on primary booking screen"
      contains: "outstanding_balance"
    - path: "lib/features/booking/confirm_booking_screen.dart"
      provides: "Balance transparency on legacy confirm screen"
      contains: "outstanding"
    - path: "lib/features/booking/widgets/vehicle_selection_widget.dart"
      provides: "Selected-category balance line under price (if card-level chosen)"
      contains: "outstanding_balance"
  key_links:
    - from: "lib/features/booking/ride_confirmation_screen.dart"
      to: "_currentFareData['outstanding_balance']"
      via: "conditional banner when balance > 0"
      pattern: "outstanding_balance"
    - from: "lib/features/booking/confirm_booking_screen.dart"
      to: "PaymentService.bookRideWithPayment success path"
      via: "unchanged booking call; isBalanceBlocked kept as dormant backstop only"
      pattern: "isBalanceBlocked"
---

<objective>
Show the unpaid-balance split on booking screens and verify silent booking needs no flow changes.

Purpose: Avoid sticker shock — rider sees why the total is higher, then books normally while Stripe auto-covers fare + debt.
Output: Banners/line items on booking surfaces; 403 handler retained as backstop, success path verified untouched.
</objective>

<execution_context>
@/Users/moksha/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/moksha/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-outstanding-balance-silent-booking-fare-transparency/INTEGRATION-GUIDE.md
@.planning/phases/15-outstanding-balance-silent-booking-fare-transparency/15-RESEARCH.md
@lib/features/booking/ride_confirmation_screen.dart
@lib/features/booking/confirm_booking_screen.dart
@lib/core/services/payment_service.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Balance transparency UI on booking surfaces</name>
  <files>lib/features/booking/ride_confirmation_screen.dart, lib/features/booking/confirm_booking_screen.dart, lib/features/booking/widgets/vehicle_selection_widget.dart</files>
  <action>RideConfirmationScreen: add getter `double get _outstandingBalance => (_currentFareData['outstanding_balance'] as num?)?.toDouble() ?? 0.0` next to `_fare`; render amber info banner above the Confirm CTA (same style family as congestion/promo banners in vehicle_selection_widget.dart:812) reading "Includes £X.XX unpaid balance from a previous ride" only when > 0. ConfirmBookingScreen: same banner reading `vehicle['outstanding_balance']` tolerance (num-or-string, default 0) above payment selector. Vehicle cards: under selected-category price, add caption "incl. £X.XX balance" when that category's fare map has balance > 0. Never subtract/add — display `total_fare` as-is. Keep promo free-ride + congestion rendering intact.</action>
  <verify><automated>flutter analyze lib/features/booking/ride_confirmation_screen.dart lib/features/booking/confirm_booking_screen.dart lib/features/booking/widgets/vehicle_selection_widget.dart</automated></verify>
  <done>Balance > 0 shows transparency copy on both booking screens + selected card; balance 0/absent renders pixel-identical to today</done>
</task>

<task type="auto">
  <name>Task 2: Verify silent-booking backstop (no flow change)</name>
  <files>lib/features/booking/ride_confirmation_screen.dart, lib/features/booking/confirm_booking_screen.dart</files>
  <action>Verify-only task — do NOT change booking request/response handling: confirm `_processBooking`/`_confirmBooking`/`_handleScheduleRide` call `PaymentService.bookRideWithPayment` unchanged (combined total comes from backend `clientSecret`/`paymentUrl`/cash fare); keep `isBalanceBlocked → _openBalanceBlocked` branches intact as dormant backstop for non-balance 403s (e.g. suspension) — update the stale `payment-flow.md §7` comment to note balance-403 no longer fires per this brief. Confirm no socket/FCM/cancel-refund edits exist in the diff. If `destination_search_screen.dart` builds fallback fareData maps that drop the new key, thread `outstanding_balance` through them.</action>
  <verify><automated>flutter test test/fare_estimate_balance_test.dart test/outstanding_balance_test.dart test/upfront_payment_contract_test.dart</automated></verify>
  <done>Booking flow unchanged, backstop comments accurate, no socket/FCM/cancel edits, contract + balance + upfront tests green</done>
</task>

</tasks>

<verification>
`flutter analyze` clean on all touched files; fare-balance + outstanding + upfront contract tests green; `git diff --stat` shows no socket/FCM/payment-service-request changes.
</verification>

<success_criteria>
Rider sees the balance split pre-booking, books normally with combined total auto-covered, zero dead-ends, zero client fare math.
</success_criteria>

<output>
After completion, create `.planning/phases/15-outstanding-balance-silent-booking-fare-transparency/15-02-SUMMARY.md`
</output>
